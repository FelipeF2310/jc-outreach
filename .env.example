# No production backend is connected in this first synthetic slice.
# npm run dev / npm run demo enable an explicit loopback-only fixture environment.
# Never put resident data, Supabase service keys, or assignment links here.
