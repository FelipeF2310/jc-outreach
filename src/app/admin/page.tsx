import { AdminSignIn } from "@/components/admin-sign-in";
// Generic shell only: identity and campaign data come from authorized no-store APIs.
export default function AdministratorPage() {
  return <AdminSignIn />;
}
